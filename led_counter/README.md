# LED Counter

4-bit binary counter displayed on 4 LEDs. Counts 0–15 and wraps around.
Each step lasts ~0.5 seconds (full cycle ≈ 8 seconds).

**Pins:** LED1=13, LED2=18, LED3=19, LED4=21

**Expected:** LEDs show an incrementing binary pattern.
